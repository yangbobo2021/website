---
// SPDX-License-Identifier: AGPL-3.0-or-later
// SPDX-FileCopyrightText: 2026 SubLang International <https://sublang.ai>

import BaseHead from '../../components/BaseHead.astro';
import Footer from '../../components/Footer.astro';
import Header from '../../components/Header.astro';
import { Image } from 'astro:assets';
import keysyncScreenshot from '../../assets/keysync-screenshot.png';
import { DEFAULT_LOCALE } from '../../i18n/config';

const PAGE_TITLE = 'KeySync — One config for every AI agent';
const PAGE_DESCRIPTION =
	'KeySync is the key and config layer your AI agents read from. Keep your providers, keys, models, and endpoints in one place and sync them to every AI agent you run — encrypted across machines, with a local backup before every write.';
const KEYSYNC_DOWNLOAD_URL = 'https://demo.signoff.bio/keysync/download';

// English-only for now. The lang switcher reports other locales as
// unavailable rather than inventing a fallback (DR-001, no fake equivalents).
const translations = { en: '/keysync/' } as const;
const alternates = (Object.entries(translations) as Array<[keyof typeof translations, string]>).map(
	([locale, path]) => ({
		locale,
		href: new URL(path, Astro.site).toString(),
	}),
);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead
			title={PAGE_TITLE}
			description={PAGE_DESCRIPTION}
			lang={DEFAULT_LOCALE}
			alternates={alternates}
		/>
	</head>
	<body>
		<Header lang={DEFAULT_LOCALE} translations={translations} />
		<main class="ks-main">
			<!-- HERO -->
			<header class="ks-band ks-hero-band">
				<div class="ks-wrap ks-hero">
					<span class="tag"><span class="ks-dot" aria-hidden="true"></span>Provider sync for AI agents</span>
					<h1>Stop re-pasting API keys into <span class="ks-hl">every</span> AI agent.</h1>
					<p class="ks-sub">
						Keep Claude Code, Codex, OpenClaw, and Hermes on the same provider
						(<b>API keys, endpoints, and models</b>) across laptops and servers.
					</p>
					<div class="ks-cta-row">
						<a href={KEYSYNC_DOWNLOAD_URL} class="button primary">
							Download KeySync
							<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path></svg>
						</a>
						<a href="#how" class="button">See how it works</a>
					</div>
					<ul class="ks-trust">
						<li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true"><path d="M20 6 9 17l-5-5"></path></svg>Backup before every write</li>
						<li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true"><path d="M20 6 9 17l-5-5"></path></svg>End-to-end encrypted</li>
						<li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true"><path d="M20 6 9 17l-5-5"></path></svg>Writes each tool's native config</li>
					</ul>

					<!-- HERO VISUAL: source of truth -> hub -> tools -->
					<div class="ks-stage">
						<div class="ks-stage-frame">
							<div class="ks-stage-grid">
								<!-- LEFT: source of truth -->
								<div class="ks-panel">
									<div class="ks-panel-label">Source of truth <span class="ks-tagged">1 place</span></div>
									<div class="ks-kv"><span class="k">anthropic</span><span class="v secret">sk-ant-••••</span></div>
									<div class="ks-kv"><span class="k">openai</span><span class="v secret">sk-••••••••</span></div>
									<div class="ks-kv"><span class="k">google</span><span class="v secret">AIza••••••</span></div>
									<div class="ks-kv"><span class="k">model</span><span class="v">claude-opus-4</span></div>
									<div class="ks-kv"><span class="k">endpoint</span><span class="v">api.proxy.dev</span></div>
								</div>

								<!-- MIDDLE: hub -->
								<div class="ks-hub">
									<div class="ks-hub-core">
										<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M21 2v6h-6"></path><path d="M3 12a9 9 0 0 1 15-6.7L21 8"></path><path d="M3 22v-6h6"></path><path d="M21 12a9 9 0 0 1-15 6.7L3 16"></path></svg>
									</div>
									<div class="ks-hub-name">KeySync</div>
								</div>

								<!-- RIGHT: tools -->
								<div class="ks-tools">
									<div class="ks-toolcard">
										<div class="ks-tool-ico">CC</div>
										<div class="ks-tool-meta"><div class="ks-tool-name">Claude Code</div><div class="ks-tool-path">~/.claude/settings.json</div></div>
										<div class="ks-synced"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" aria-hidden="true"><path d="M20 6 9 17l-5-5"></path></svg>synced</div>
									</div>
									<div class="ks-toolcard">
										<div class="ks-tool-ico">OC</div>
										<div class="ks-tool-meta"><div class="ks-tool-name">OpenClaw</div><div class="ks-tool-path">~/.openclaw/openclaw.json</div></div>
										<div class="ks-synced"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" aria-hidden="true"><path d="M20 6 9 17l-5-5"></path></svg>synced</div>
									</div>
									<div class="ks-toolcard">
										<div class="ks-tool-ico">He</div>
										<div class="ks-tool-meta"><div class="ks-tool-name">Hermes</div><div class="ks-tool-path">~/.hermes/config.yaml</div></div>
										<div class="ks-synced"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" aria-hidden="true"><path d="M20 6 9 17l-5-5"></path></svg>synced</div>
									</div>
									<div class="ks-toolcard">
										<div class="ks-tool-ico">Cx</div>
										<div class="ks-tool-meta"><div class="ks-tool-name">Codex CLI</div><div class="ks-tool-path">~/.codex/config.toml</div></div>
										<div class="ks-synced"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" aria-hidden="true"><path d="M20 6 9 17l-5-5"></path></svg>synced</div>
									</div>
								</div>

								<!-- animated connectors -->
								<svg class="ks-flow" preserveAspectRatio="none" viewBox="0 0 1000 260" aria-hidden="true">
									<line x1="320" y1="130" x2="445" y2="130"></line>
									<line x1="555" y1="130" x2="680" y2="60"></line>
									<line x1="555" y1="130" x2="680" y2="107"></line>
									<line x1="555" y1="130" x2="680" y2="153"></line>
									<line x1="555" y1="130" x2="680" y2="200"></line>
								</svg>
							</div>
						</div>
					</div>
				</div>
			</header>

			<!-- PROBLEM -->
			<section class="ks-band ks-problem">
				<div class="ks-wrap ks-problem-grid">
					<div>
						<div class="ks-sec-eyebrow">Provider drift</div>
						<h2 class="ks-sec-title">Provider configs drift across machines.</h2>
						<ul class="ks-pain-list">
							<li><span>Your CC Switch setup works on one machine, then every <b>new laptop or server</b> starts from zero.</span></li>
							<li><span>Mac, Windows, Linux, desktop, notebook — API keys, endpoints, and models <b>fall out of sync</b>.</span></li>
							<li><span>Team setup turns into <b>copied secrets in chat</b> when someone needs the same key or endpoint.</span></li>
							<li><span>When a provider rate-limits while you're away, switching models still depends on <b>the right machine</b>.</span></li>
						</ul>
					</div>
					<div class="ks-drift-visual" aria-label="Illustration of provider configurations drifting across devices">
						<div class="ks-drift-devices">
							<!-- Work Laptop - just updated -->
							<div class="ks-device-card ks-card-synced">
								<div class="ks-device-header">
									<div class="ks-device-info">
										<div class="ks-device-icon ks-icon-laptop" aria-hidden="true">
											<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="4" width="18" height="12" rx="2"/><path d="M1 20h22"/></svg>
										</div>
										<span class="ks-device-name">Work Laptop</span>
									</div>
									<span class="ks-device-status ks-status-just-updated">
										<svg class="ks-status-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M20 6 9 17l-5-5"/></svg>
										'Key-old' updated to 'Key-new'
									</span>
								</div>
								<div class="ks-device-rows">
									<div class="ks-device-row">
										<span class="ks-tool-name">Claude Code <span class="ks-tool-key ks-key-new">key-new</span></span>
										<span class="ks-tool-status ks-status-connected">connected</span>
									</div>
									<div class="ks-row-divider"></div>
									<div class="ks-device-row">
										<span class="ks-tool-name">Codex <span class="ks-tool-key ks-key-new">key-new</span></span>
										<span class="ks-tool-status ks-status-connected">connected</span>
									</div>
									<div class="ks-row-divider"></div>
									<div class="ks-device-row">
										<span class="ks-tool-name">OpenClaw <span class="ks-tool-key ks-key-new">key-new</span></span>
										<span class="ks-tool-status ks-status-connected">connected</span>
									</div>
									<div class="ks-row-divider"></div>
									<div class="ks-device-row">
										<span class="ks-tool-name">Hermes <span class="ks-tool-key ks-key-new">key-new</span></span>
										<span class="ks-tool-status ks-status-connected">connected</span>
									</div>
								</div>
							</div>

							<!-- Home Desktop - not synced -->
							<div class="ks-device-card ks-card-stale">
								<div class="ks-device-header">
									<div class="ks-device-info">
										<div class="ks-device-icon ks-icon-desktop" aria-hidden="true">
											<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8"/><path d="M12 17v4"/></svg>
										</div>
										<span class="ks-device-name">Home Desktop</span>
									</div>
									<span class="ks-device-status ks-status-not-synced">
										<svg class="ks-status-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
										Not Synced
									</span>
								</div>
								<div class="ks-device-rows">
									<div class="ks-device-row">
										<span class="ks-tool-name">Codex <span class="ks-tool-key ks-key-old">key-old</span></span>
										<span class="ks-tool-status ks-status-disconnected">disconnected</span>
									</div>
									<div class="ks-row-divider"></div>
									<div class="ks-device-row">
										<span class="ks-tool-name">Hermes <span class="ks-tool-key ks-key-old">key-old</span></span>
										<span class="ks-tool-status ks-status-disconnected">disconnected</span>
									</div>
								</div>
							</div>

							<!-- Server - not synced -->
							<div class="ks-device-card ks-card-stale">
								<div class="ks-device-header">
									<div class="ks-device-info">
										<div class="ks-device-icon ks-icon-server" aria-hidden="true">
											<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="2" width="20" height="8" rx="2"/><rect x="2" y="14" width="20" height="8" rx="2"/><circle cx="6" cy="6" r="1"/><circle cx="6" cy="18" r="1"/></svg>
										</div>
										<span class="ks-device-name">Server</span>
									</div>
									<span class="ks-device-status ks-status-not-synced">
										<svg class="ks-status-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg>
										Not Synced
									</span>
								</div>
								<div class="ks-device-rows">
									<div class="ks-device-row">
										<span class="ks-tool-name">Claude Code <span class="ks-tool-key ks-key-old">key-old</span></span>
										<span class="ks-tool-status ks-status-disconnected">disconnected</span>
									</div>
									<div class="ks-row-divider"></div>
									<div class="ks-device-row">
										<span class="ks-tool-name">Codex <span class="ks-tool-key ks-key-old">key-old</span></span>
										<span class="ks-tool-status ks-status-disconnected">disconnected</span>
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</section>

			<!-- HOW IT WORKS -->
			<section class="ks-band" id="how">
				<div class="ks-wrap">
					<div class="ks-sec-head">
						<div class="ks-sec-eyebrow">How it works</div>
						<h2 class="ks-sec-title">Three steps. Then never touch a config file again.</h2>
					</div>
					<div class="ks-steps">
						<div class="ks-step">
							<div class="ks-step-n">1</div>
							<h3>Build a provider profile</h3>
							<p>Bundle your API keys, default models, and custom endpoints into one profile — Anthropic, OpenAI, Google, OpenRouter, or your own local gateway.</p>
						</div>
						<div class="ks-step">
							<div class="ks-step-n">2</div>
							<h3>Pick your tools</h3>
							<p>Choose which agents and tools KeySync manages: <code>Claude Code</code>, <code>Codex</code>, <code>OpenClaw</code>, <code>Hermes</code>, and more.</p>
						</div>
						<div class="ks-step">
							<div class="ks-step-n">3</div>
							<h3>Sync everywhere</h3>
							<p>KeySync writes each tool's native config for you. Change a key once and it lands in every tool, on every machine.</p>
						</div>
					</div>

					<div class="ks-guarantees" id="features" aria-label="What stays true after each sync">
						<div class="ks-guarantees-head">
							<div class="ks-sub-eyebrow">What stays true</div>
							<p>Four guarantees carry through the workflow, no matter which provider or agent you connect.</p>
						</div>
						<ul class="ks-guarantee-list">
							<li>
								<h3>One source of truth</h3>
								<p>Keys, models, and endpoints live in one profile, so rotations happen once.</p>
							</li>
							<li>
								<h3>Native to every agent</h3>
								<p>KeySync writes each agent's own format and file location, with no wrapper to learn.</p>
							</li>
							<li>
								<h3>Encrypted, everywhere</h3>
								<p>Keys are encrypted on device before sync; the cloud only stores ciphertext.</p>
							</li>
							<li>
								<h3>Safe by default</h3>
								<p>Every write starts with a local backup, and apply reports show what synced.</p>
							</li>
						</ul>
					</div>
				</div>
			</section>

			<!-- SECURITY -->
			<section class="ks-band ks-security" id="security">
				<div class="ks-wrap ks-sec-split">
					<div>
						<div class="ks-sec-eyebrow">Built for handling secrets</div>
						<h2 class="ks-sec-title">Sync keys without losing control.</h2>
						<p class="ks-sec-lead">Secrets are encrypted before they leave your machine. Local configs are backed up before every write.</p>
						<ul class="ks-check-list">
							<li><span class="ks-ck" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M20 6 9 17l-5-5"></path></svg></span><div><div class="t">Encrypted before it leaves your machine</div><div class="d">Your keys are encrypted client-side — the cloud only ever stores ciphertext.</div></div></li>
							<li><span class="ks-ck" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M20 6 9 17l-5-5"></path></svg></span><div><div class="t">Local backup before every write</div><div class="d">Each tool's existing config is saved locally first, so a sync can never silently overwrite your setup.</div></div></li>
							<li><span class="ks-ck" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M20 6 9 17l-5-5"></path></svg></span><div><div class="t">Visible status instead of silent failure</div><div class="d">Apply reports flag which devices synced and which still need attention.</div></div></li>
							<li><span class="ks-ck" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><path d="M20 6 9 17l-5-5"></path></svg></span><div><div class="t">Writes the explicit config files your tools already read</div><div class="d">Known files in their own formats — no vague compatibility claim.</div></div></li>
						</ul>
					</div>
					<!-- IMAGE SLOT: drop a real product screenshot here (e.g. the Web Console Providers list or the device status view) -->
					<div class="ks-imgslot">
						<Image
							src={keysyncScreenshot}
							alt="KeySync web console screenshot showing synced provider configuration"
							class="ks-shot"
						/>
					</div>
				</div>
			</section>

			<!-- FAQ -->
			<section class="ks-band" id="faq">
				<div class="ks-wrap">
					<div class="ks-sec-head ks-center">
						<div class="ks-sec-eyebrow">Questions</div>
						<h2 class="ks-sec-title ks-mx-auto">Good things to know</h2>
					</div>
					<div class="ks-faq">
						<details open>
							<summary>Which AI agents and tools does it support?<span class="ks-plus" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path class="ks-plus-bar" d="M12 5v14"></path></svg></span></summary>
							<p>The ones you already run — Claude Code, Codex, OpenClaw, and Hermes, with more added regularly. KeySync writes each one's native config file, so support is really just about knowing the format.</p>
						</details>
						<details>
							<summary>How are my API keys stored?<span class="ks-plus" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path class="ks-plus-bar" d="M12 5v14"></path></svg></span></summary>
							<p>Keys are encrypted on your machine before they're ever uploaded. The cloud stores only encrypted data so your setup can follow you across devices — but your secrets stay yours.</p>
						</details>
						<details>
							<summary>What if a sync breaks my config?<span class="ks-plus" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path class="ks-plus-bar" d="M12 5v14"></path></svg></span></summary>
							<p>It won't silently. KeySync saves a local backup of each file before writing, and the apply report flags any device that didn't sync cleanly — so failures are visible rather than silent.</p>
						</details>
						<details>
							<summary>Can I use custom endpoints or a local gateway?<span class="ks-plus" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path class="ks-plus-bar" d="M12 5v14"></path></svg></span></summary>
							<p>Yes. Set a custom base URL per provider — a proxy, a self-hosted gateway, or a local model server — and KeySync points every tool at it.</p>
						</details>
						<details>
							<summary>Do my agents need to change anything?<span class="ks-plus" aria-hidden="true"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"></path><path class="ks-plus-bar" d="M12 5v14"></path></svg></span></summary>
							<p>No. KeySync writes the same config files your agents already read. There's nothing for them to integrate with and nothing new to learn — KeySync is the key and config layer beneath them, not another agent.</p>
						</details>
					</div>
				</div>
			</section>

			<!-- FINAL CTA -->
			<section class="ks-band ks-final" id="get-started">
				<div class="ks-wrap">
					<div class="ks-final-card">
						<h2>Set it once. <span class="ks-hl">Sync it everywhere.</span></h2>
						<p>Download the desktop client, connect your first provider, and push your config to every AI agent in under a minute.</p>
						<div class="ks-cta-row ks-center">
							<a href={KEYSYNC_DOWNLOAD_URL} class="button primary">
								Download KeySync
								<svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"></path><path d="m12 5 7 7-7 7"></path></svg>
							</a>
							<a href="#how" class="button">See how it works</a>
						</div>
						<div class="ks-run">$ ./keysync &nbsp;·&nbsp; requires Node.js 24+</div>
					</div>
				</div>
			</section>
		</main>
		<Footer lang={DEFAULT_LOCALE} />
	</body>

	<style>
	/* KeySync subsite — ported into the SubLang light design system.
	   Reuses global tokens (--ink, --accent, --line, --surface, fonts) and the
	   shared .button / .tag / .card styles; ks- classes add layout local to
	   this page. */
	.ks-main {
		--ks-mono: var(--font-mono);
		--ks-page: #fdfcf9;
		--ks-surface-2: #faf9f5;
		--ks-line: #ece9e2;
		--ks-shadow-card: 0 14px 34px rgba(21, 22, 26, 0.05);
		--ks-shadow-float: 0 22px 56px rgba(21, 22, 26, 0.075);
		--ks-accent-soft: rgba(137, 15, 188, 0.09);
		width: 100%;
		max-width: none;
		margin: 0;
		padding: 0;
		background: var(--ks-page);
	}
	:global(.site-header) {
		background: rgba(253, 252, 249, 0.86);
		border-bottom-color: var(--ks-line);
	}

	/* Full-width section bands with a centered inner column. */
	.ks-band {
		padding: 5.5rem 0;
		border-top: 1px solid var(--ks-line);
	}
	.ks-wrap {
		max-width: var(--grid-max);
		margin: 0 auto;
		padding: 0 2rem;
	}

	/* ---------- HERO ---------- */
	.ks-hero-band {
		border-top: none;
		padding: 4.5rem 0 4rem;
		text-align: center;
	}
	.ks-dot {
		width: 7px;
		height: 7px;
		border-radius: 50%;
		background: var(--accent);
		animation: ks-pulse 2s infinite;
	}
	@keyframes ks-pulse {
		0%, 100% { opacity: 1; }
		50% { opacity: 0.35; }
	}
	.ks-hero h1 {
		font-size: clamp(2.45rem, 5.5vw, 4.05rem);
		font-weight: 600;
		line-height: 1.04;
		letter-spacing: -0.025em;
		max-width: 16ch;
		margin: 1.4rem auto 1.3rem;
	}
	.ks-hl {
		color: var(--accent);
	}
	.ks-sub {
		font-size: clamp(1.03rem, 1.35vw, 1.16rem);
		line-height: 1.58;
		color: var(--ink-soft);
		max-width: 55ch;
		margin: 0 auto 2rem;
	}
	.ks-sub b {
		color: var(--ink);
		font-weight: 600;
	}
	.ks-cta-row {
		display: flex;
		gap: 1rem;
		justify-content: center;
		flex-wrap: wrap;
		margin-bottom: 1.6rem;
	}
	.ks-trust {
		list-style: none;
		display: flex;
		gap: 1.4rem;
		justify-content: center;
		flex-wrap: wrap;
		margin: 0;
		padding: 0;
		font-family: var(--font-display);
		font-size: 0.8rem;
		font-weight: 500;
		letter-spacing: 0.04em;
		color: var(--ink-muted);
	}
	.ks-trust li {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
	}
	.ks-trust svg {
		width: 15px;
		height: 15px;
		color: var(--accent);
	}

	/* ---------- HERO VISUAL ---------- */
	.ks-stage {
		max-width: 1000px;
		margin: 3.5rem auto 0;
		text-align: left;
	}
	.ks-stage-frame {
		position: relative;
		overflow: hidden;
		border: 1px solid var(--ks-line);
		border-radius: var(--radius-lg);
		background: var(--surface);
		padding: 1.8rem;
		box-shadow: var(--ks-shadow-float);
	}
	.ks-stage-grid {
		position: relative;
		display: grid;
		grid-template-columns: 1fr auto 1fr;
		align-items: center;
	}
	.ks-panel {
		position: relative;
		z-index: 2;
		background: var(--ks-surface-2);
		border: 1px solid var(--ks-line);
		border-radius: var(--radius-md);
		padding: 1rem 1rem 0.85rem;
	}
	.ks-panel-label {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		font-family: var(--font-display);
		font-weight: 600;
		font-size: 0.66rem;
		letter-spacing: 0.16em;
		text-transform: uppercase;
		color: var(--ink-muted);
		margin-bottom: 0.8rem;
	}
	.ks-tagged {
		margin-left: auto;
		color: var(--accent);
		text-transform: none;
		letter-spacing: 0.04em;
	}
	.ks-kv {
		display: flex;
		align-items: center;
		justify-content: space-between;
		font-family: var(--ks-mono);
		font-size: 0.78rem;
		padding: 0.45rem 0;
		border-bottom: 1px dashed var(--ks-line);
	}
	.ks-kv:last-child { border-bottom: none; }
	.ks-kv .k { color: var(--ink-muted); }
	.ks-kv .v { color: var(--ink); }
	.ks-kv .secret { color: var(--accent); letter-spacing: 0.1em; }

	/* hub */
	.ks-hub {
		position: relative;
		z-index: 3;
		display: flex;
		flex-direction: column;
		align-items: center;
		gap: 0.6rem;
		padding: 0 1.1rem;
	}
	.ks-hub-core {
		width: 58px;
		height: 58px;
		border-radius: var(--radius-md);
		display: grid;
		place-items: center;
		color: var(--accent);
		background: rgba(137, 15, 188, 0.07);
		border: 1px solid rgba(137, 15, 188, 0.16);
		box-shadow: none;
	}
	.ks-hub-core svg { width: 28px; height: 28px; }
	.ks-hub-name {
		font-family: var(--font-display);
		font-size: 0.72rem;
		font-weight: 700;
		letter-spacing: 0.02em;
		color: var(--ink);
	}

	/* tool cards */
	.ks-tools {
		display: flex;
		flex-direction: column;
		gap: 0.65rem;
	}
	.ks-toolcard {
		position: relative;
		z-index: 2;
		display: flex;
		align-items: center;
		gap: 0.7rem;
		background: var(--ks-surface-2);
		border: 1px solid var(--ks-line);
		border-radius: var(--radius-md);
		padding: 0.7rem 0.8rem;
	}
	.ks-tool-ico {
		flex-shrink: 0;
		width: 30px;
		height: 30px;
		border-radius: var(--radius-sm);
		display: grid;
		place-items: center;
		background: var(--surface);
		border: 1px solid var(--ks-line);
		font-family: var(--ks-mono);
		font-weight: 700;
		font-size: 0.78rem;
		color: var(--ink);
	}
	.ks-tool-meta { flex: 1; min-width: 0; }
	.ks-tool-name { font-weight: 600; font-size: 0.86rem; color: var(--ink); }
	.ks-tool-path {
		font-family: var(--ks-mono);
		font-size: 0.66rem;
		color: var(--ink-muted);
		white-space: nowrap;
		overflow: hidden;
		text-overflow: ellipsis;
	}
	.ks-synced {
		flex-shrink: 0;
		display: flex;
		align-items: center;
		gap: 0.3rem;
		font-family: var(--font-display);
		font-size: 0.68rem;
		font-weight: 600;
		color: var(--accent);
	}
	.ks-synced svg { width: 12px; height: 12px; }

	/* connectors */
	.ks-flow {
		position: absolute;
		inset: 0;
		width: 100%;
		height: 100%;
		z-index: 1;
		pointer-events: none;
	}
	.ks-flow line {
		stroke: rgba(137, 15, 188, 0.3);
		stroke-width: 1.4;
		stroke-dasharray: 5 7;
	}

	@media (max-width: 860px) {
		.ks-stage-grid { grid-template-columns: 1fr; gap: 1.1rem; }
		.ks-hub { flex-direction: row; justify-content: center; padding: 0.4rem 0; }
		.ks-flow { display: none; }
	}

	/* ---------- SECTION SHELL ---------- */
	.ks-sec-eyebrow {
		font-family: var(--font-display);
		font-size: 0.72rem;
		font-weight: 600;
		letter-spacing: 0.18em;
		text-transform: uppercase;
		color: var(--accent-dark);
		margin-bottom: 0.8rem;
	}
	.ks-sec-title {
		max-width: 18ch;
		font-weight: 600;
		line-height: 1.08;
		letter-spacing: -0.018em;
	}
	.ks-sec-lead {
		color: var(--ink-soft);
		font-size: 1.05rem;
		line-height: 1.6;
		max-width: 50ch;
		margin-top: 1rem;
	}
	.ks-sec-head { margin-bottom: 3rem; }
	.ks-center { text-align: center; }
	.ks-mx-auto { margin-left: auto; margin-right: auto; }

	/* ---------- PROBLEM ---------- */
	.ks-problem-grid {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3.4rem;
		align-items: center;
	}
	.ks-pain-list {
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: 0.9rem;
		margin: 1.4rem 0 0;
		padding: 0;
	}
	.ks-pain-list li {
		display: grid;
		grid-template-columns: 26px minmax(0, 1fr);
		column-gap: 0.8rem;
		align-items: flex-start;
		color: var(--ink-soft);
		font-size: 1rem;
		line-height: 1.58;
	}
	.ks-pain-list li::before {
		content: "!";
		flex-shrink: 0;
		width: 26px;
		height: 26px;
		border-radius: var(--radius-sm);
		display: grid;
		place-items: center;
		margin-top: 0.08rem;
		background: rgba(190, 71, 56, 0.075);
		border: 1px solid rgba(190, 71, 56, 0.16);
		color: #b24a3a;
		font-family: var(--font-display);
		font-size: 0.8rem;
		font-weight: 700;
		line-height: 1;
	}
	.ks-pain-list b { color: var(--ink); font-weight: 600; }
	.ks-drift-visual {
		position: relative;
		border: none;
		border-radius: var(--radius-lg);
		overflow: visible;
	}
	.ks-drift-devices {
		display: flex;
		flex-direction: column;
		gap: 1rem;
	}
	.ks-device-card {
		background: var(--surface);
		border: 1px solid var(--line);
		border-radius: 12px;
		padding: 1.5rem;
		transition: border-color 0.2s ease, box-shadow 0.2s ease;
	}
	/* Synced card - neutral styling, Key badge conveys "new" */
	.ks-card-synced {
		border-color: var(--line);
	}
	/* Stale card - neutral */
	.ks-card-stale {
		border-color: var(--line);
		opacity: 0.85;
	}
	.ks-device-header {
		display: flex;
		align-items: center;
		justify-content: space-between;
		margin-bottom: 1.5rem;
	}
	.ks-device-info {
		display: flex;
		align-items: center;
		gap: 0.625rem;
	}
	.ks-device-icon {
		width: 30px;
		height: 30px;
		display: flex;
		align-items: center;
		justify-content: center;
		background: rgba(137, 15, 188, 0.08);
		border-radius: 6px;
	}
	.ks-device-icon svg {
		width: 20px;
		height: 20px;
		color: var(--accent);
	}
	.ks-device-name {
		font-family: var(--font-display);
		font-size: 0.9rem;
		font-weight: 500;
		color: var(--ink);
	}
	.ks-device-status {
		font-family: var(--font-display);
		font-size: 0.8rem;
		font-weight: 600;
		letter-spacing: 0.02em;
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
	}
	.ks-status-icon {
		width: 14px;
		height: 14px;
		flex-shrink: 0;
	}
	.ks-status-just-updated {
		color: #2d7d46;
	}
	.ks-status-not-synced {
		color: var(--ink-muted);
	}
	.ks-device-key {
		display: flex;
		align-items: center;
	}
	.ks-status-synced {
		color: var(--accent);
		font-weight: 600;
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
	}
	.ks-status-synced svg {
		width: 16px;
		height: 16px;
	}
	.ks-status-not-synced {
		color: var(--ink-muted);
		font-weight: 500;
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
	}
	.ks-status-not-synced svg {
		width: 16px;
		height: 16px;
	}
	.ks-status-stale {
		color: var(--ink-muted);
	}
	.ks-device-rows {
		display: flex;
		flex-direction: column;
		gap: 0.75rem;
	}
	.ks-device-row {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}
	.ks-tool-name {
		font-family: var(--font-display);
		font-size: 0.78rem;
		font-weight: 500;
		color: var(--ink-soft);
		display: flex;
		align-items: center;
		gap: 0.4rem;
	}
	.ks-tool-key {
		font-family: var(--font-display);
		font-size: 0.6rem;
		font-weight: 600;
		padding: 0.12rem 0.35rem;
		border-radius: 4px;
		letter-spacing: 0.02em;
	}
	.ks-key-new {
		background: rgba(137, 15, 188, 0.08);
		color: var(--accent);
	}
	.ks-key-old {
		background: rgba(21, 22, 26, 0.06);
		color: var(--ink-soft);
	}
	.ks-tool-status {
		font-family: var(--font-display);
		font-size: 0.72rem;
		letter-spacing: 0.02em;
		font-weight: 500;
	}
	.ks-status-connected {
		color: #2d7d46;
	}
	.ks-status-disconnected {
		color: var(--ink-muted);
	}
	.ks-row-divider {
		height: 1px;
		background: var(--line);
	}

	@media (max-width: 640px) {
		/* Stack cards vertically on mobile */
	}

	/* ---------- HOW IT WORKS ---------- */
	.ks-steps {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1.4rem;
	}
	.ks-step {
		background: rgba(255, 255, 255, 0.72);
		border: 1px solid var(--ks-line);
		border-radius: var(--radius-lg);
		padding: 1.7rem 1.5rem;
		box-shadow: none;
		transition: transform 0.25s ease, border-color 0.25s ease, background-color 0.25s ease;
	}
	.ks-step:hover {
		transform: translateY(-3px);
		border-color: rgba(137, 15, 188, 0.35);
		background: var(--surface);
	}
	.ks-step-n {
		display: grid;
		place-items: center;
		width: 34px;
		height: 34px;
		margin-bottom: 1.1rem;
		border-radius: var(--radius-sm);
		border: 1px solid rgba(137, 15, 188, 0.18);
		background: var(--surface);
		font-family: var(--ks-mono);
		font-weight: 700;
		font-size: 0.85rem;
		color: var(--accent);
	}
	.ks-step h3 { font-size: 1.2rem; margin-bottom: 0.5rem; }
	.ks-step p { color: var(--ink-soft); font-size: 0.96rem; margin: 0; }
	.ks-step p code {
		font-family: var(--ks-mono);
		font-size: 0.85em;
		color: var(--ink);
		background: rgba(21, 22, 26, 0.06);
		padding: 1px 6px;
		border-radius: 6px;
	}

	.ks-guarantees {
		display: grid;
		grid-template-columns: minmax(14rem, 0.68fr) minmax(0, 1.32fr);
		gap: 2rem;
		margin-top: 2.4rem;
		padding-top: 2rem;
		border-top: 1px solid var(--ks-line);
	}
	.ks-guarantees-head p {
		max-width: 28ch;
		margin: 0;
		color: var(--ink-soft);
		font-size: 0.98rem;
	}
	.ks-sub-eyebrow {
		font-family: var(--font-display);
		font-size: 0.72rem;
		font-weight: 600;
		letter-spacing: 0.18em;
		text-transform: uppercase;
		color: var(--accent-dark);
		margin-bottom: 0.75rem;
	}
	.ks-guarantee-list {
		list-style: none;
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1.2rem 1.6rem;
		margin: 0;
		padding: 0;
	}
	.ks-guarantee-list li {
		padding-top: 0.9rem;
		border-top: 1px solid var(--ks-line);
	}
	.ks-guarantee-list li:nth-child(1),
	.ks-guarantee-list li:nth-child(2) {
		padding-top: 0;
		border-top: none;
	}
	.ks-guarantee-list h3 {
		margin-bottom: 0.35rem;
		font-size: 1.05rem;
	}
	.ks-guarantee-list p {
		margin: 0;
		color: var(--ink-soft);
		font-size: 0.94rem;
		line-height: 1.55;
	}

	/* ---------- SECURITY ---------- */
	.ks-sec-split {
		display: grid;
		grid-template-columns: 1fr 1fr;
		gap: 3.5rem;
		align-items: center;
	}
	.ks-check-list {
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: 1.1rem;
		margin: 1.6rem 0 0;
		padding: 0;
	}
	.ks-check-list li {
		display: flex;
		gap: 0.9rem;
		align-items: flex-start;
	}
	.ks-ck {
		flex-shrink: 0;
		width: 26px;
		height: 26px;
		border-radius: var(--radius-sm);
		display: grid;
		place-items: center;
		background: var(--ks-accent-soft);
		border: 1px solid rgba(137, 15, 188, 0.16);
		color: var(--accent);
	}
	.ks-ck svg { width: 14px; height: 14px; }
	.ks-check-list .t { font-weight: 600; font-size: 1.04rem; color: var(--ink); margin-bottom: 0.15rem; }
	.ks-check-list .d { color: var(--ink-soft); font-size: 0.94rem; }

	.ks-imgslot {
		overflow: hidden;
		border: 1px solid var(--ks-line);
		border-radius: var(--radius-md);
		background: var(--surface);
		box-shadow: var(--ks-shadow-card);
	}
	.ks-shot {
		display: block;
		width: 100%;
		height: auto;
		border-radius: var(--radius-md);
	}

	/* ---------- FAQ ---------- */
	.ks-faq {
		max-width: 760px;
		margin: 0 auto;
	}
	.ks-faq details {
		border-bottom: 1px solid var(--ks-line);
		padding: 0.4rem 0;
	}
	.ks-faq summary {
		list-style: none;
		cursor: pointer;
		display: flex;
		justify-content: space-between;
		align-items: center;
		gap: 1rem;
		padding: 1.1rem 0.25rem;
		font-family: var(--font-display);
		font-weight: 600;
		font-size: 1.08rem;
		color: var(--ink);
	}
	.ks-faq summary::-webkit-details-marker { display: none; }
	.ks-plus {
		flex-shrink: 0;
		width: 24px;
		height: 24px;
		display: grid;
		place-items: center;
		border: 1px solid var(--ks-line);
		border-radius: var(--radius-sm);
		color: var(--accent);
	}
	.ks-plus svg { width: 14px; height: 14px; }
	.ks-plus-bar { transition: opacity 0.2s ease; }
	.ks-faq details[open] .ks-plus-bar { opacity: 0; }
	.ks-faq details p {
		margin: 0;
		padding: 0 0.25rem 1.2rem;
		color: var(--ink-soft);
		font-size: 0.98rem;
		max-width: 64ch;
	}

	/* ---------- FINAL CTA ---------- */
	.ks-final {
		text-align: center;
	}
	.ks-final-card {
		position: relative;
		overflow: hidden;
		border: 1px solid var(--ks-line);
		border-radius: var(--radius-lg);
		background: var(--surface);
		padding: 4rem 2.5rem;
		box-shadow: var(--ks-shadow-float);
	}
	.ks-final-card h2 {
		max-width: 16ch;
		margin: 0 auto 1.1rem;
		font-size: clamp(2rem, 4.4vw, 3.1rem);
		font-weight: 600;
		letter-spacing: -0.018em;
	}
	.ks-final-card p {
		max-width: 50ch;
		margin: 0 auto 2rem;
		color: var(--ink-soft);
		font-size: 1.1rem;
	}
	.ks-run {
		margin-top: 1.4rem;
		font-family: var(--ks-mono);
		font-size: 0.82rem;
		color: var(--ink-muted);
	}

	/* ---------- RESPONSIVE ---------- */
	@media (max-width: 900px) {
		.ks-band { padding: 4rem 0; }
		.ks-wrap { padding: 0 1.5rem; }
	}
	@media (max-width: 840px) {
		.ks-problem-grid,
		.ks-sec-split { grid-template-columns: 1fr; gap: 2.4rem; }
		.ks-steps { grid-template-columns: 1fr; }
		.ks-guarantees { grid-template-columns: 1fr; gap: 1.4rem; }
		.ks-guarantees-head p { max-width: 42ch; }
		.ks-drift-visual { max-width: 620px; width: 100%; margin: 0 auto; }
	}
	@media (max-width: 720px) {
		.ks-wrap { padding: 0 1.2rem; }
		.ks-cta-row { flex-direction: column; align-items: stretch; }
		.ks-trust { gap: 0.8rem 1.2rem; }
		.ks-drift-visual { max-width: 100%; }
		.ks-device-card { padding: 1.25rem; }
	}
	@media (prefers-reduced-motion: reduce) {
		.ks-dot { animation: none; }
	}
</style>
</html>
